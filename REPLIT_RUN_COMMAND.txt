Replit Deploy → your deployment → Adjust settings → General configuration:

  Build command:  LEAVE EMPTY (optional field)
  Run command:    cd server && bash run.sh

If Build command is set to "cd server && bash run.sh", clear it. Build runs in a different environment and will fail; only Run command should start the server. Then save and Republish.
