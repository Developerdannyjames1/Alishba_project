Replit Deploy → your deployment → Adjust settings → General configuration:

  Build command:  LEAVE EMPTY (optional field)
  Run command:    Try in this order:
    1) npm run start:server   (Replit often provides Node only when run command uses npm from repo root)
    2) cd server && bash run.sh   (if you see "node: not found", switch back to option 1)

Add Production secrets: MONGO_URI, JWT_SECRET, CLIENT_URL. Then Republish.
